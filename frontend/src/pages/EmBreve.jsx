function EmBreve({ titulo }) {
  return (
    <section className="container" style={{ padding: '96px 24px' }}>
      <h2>{titulo}</h2>
      <p>Esta página ainda está por construir.</p>
    </section>
  )
}

export default EmBreve
